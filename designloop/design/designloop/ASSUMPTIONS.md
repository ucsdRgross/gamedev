# ASSUMPTIONS — designloop

Decisions made during implementation that the design did not cover, and that were judged
**reversible and clearly within intent** (the ASSUME row of the gap protocol in `PLAN.md`).

Anything that is *not* reversible-and-obvious belongs in `gaps/`, not here.

One line per assumption. Cite the design node or plan step you were working on.

| Date | Step / node | Assumption | Why it is reversible |
|---|---|---|---|
| 2026-08-01 | S2 / §5.1 | A `- **Qn**` bullet with **no backticked gate** that is not the retired form is prose, not a question, and is skipped silently (counted in `ignored`). Both design documents carry such bullets outside their questionnaires (`SPOTLIGHT_DESIGN.md` §7 lists Q61/Q63/Q64/Q66 as open prose forks). Treating them as broken questions would report errors in a correct document. | One predicate in `parseQuestionLine`; `ignored` is already returned so the alternative rule is a one-line change. |
| 2026-08-01 | S2 / §5.6 | The acceptance count is read as **188 `Q`-numbered questions + 8 `QR` root gates = 196 lines**, not "188 total of which 8 are QR". `SPOTLIGHT_DESIGN.md` runs Q1–Q188 (Q140 retired in place) plus QR1–QR8; the plan's "188 questions, 8 of them `QR*`" cannot both be true of that document. | It is a test assertion, not behaviour. |
| 2026-08-01 | S2 / §5.5 | Section **gate weight** counts the questions transitively pruned by the questions a section's own gate names; an ungated section weighs 0 and ties keep document order. That is the literal §5.5 rule; the visible consequence is that §17.0 (ungated, weight 0) still comes first because everything heavier is PENDING until its gate is answered. | One function (`sectionWeights`), and `nextQuestion` is its only caller. |
| 2026-08-01 | S3 / Q104 | Spotlight is registered **in place** for now: `solatro/design/spotlight/meta.json` exists with `"doc": "../../SPOTLIGHT_DESIGN.md"`, so the index and the question screen have a real 196-question design to be verified against without the owner's paused document being moved or edited. Q104=(a) still stands — S16 performs the real move and changes that one string to `DESIGN.md`. | One JSON value, and the file it points at has not been touched. |
| 2026-08-01 | S3 / Q107, §2 | A design's **display title and archived flag live in `ui_meta.json`** (UI-owned), overriding `meta.json`'s title. Renaming from the index (Q107=a) must not write `meta.json`, which §2 gives to the agent — one writer per file is the entire concurrency design. | One field lookup in `readDesign`. |
| 2026-08-01 | S8 / §4.2, §4.8 | The handshake's last leg needs an endpoint §4.8 does not list: **`POST /api/designs/:key/resume`**, which sets the owner half back to `answering` at the agent's round. §4.2 says "UI sees it, switches screen, sets owner `answering`", and the owner half is UI-owned, so the UI can only write it through the server. | One route; no file format changes. |
| 2026-08-01 | S8 / Q29 | `status.agent.json` may carry an optional **`summary`** string, which the UI shows as the banner opening a new round ("your answer to Q31 opened these"). Q29=(a) requires the summary; §4.2's schema has nowhere to put it. The file has exactly one writer, so an extra field cannot race anything. | Additive and optional — a missing `summary` just means no banner. |
| 2026-08-01 | S4 / §4 | `writeJsonAtomic` **retries a replacing rename** on EPERM/EACCES/EBUSY (20 attempts, short backoff). Measured, not theorised: on Windows the rename fails while the agent's watch (S9) holds the design directory open, which this design guarantees will happen. The write is still atomic; only the instant it lands moves. | One helper; delete the loop to get the plain rename back. |
| 2026-08-01 | S3 / testing | `DESIGNLOOP_ROOT` overrides the scanned repo root, so the API tests point at a throwaway tree instead of writing fixture projects into the repo under test. Unset in normal use. | One `??` in `server.mjs`. |
| 2026-08-01 | S2 / §4.8 | The static server serves only `web/` and `src/`, with `/` redirecting to `/web/index.html`, so the browser's `../src/grammar.mjs` import resolves without the tool directory being exposed wholesale. | Path handling in one function (`resolveStatic`). |
| 2026-08-01 | S11 / §6 | The chart corpus is **25**, not the 24 §6 states: `SPOTLIGHT_DESIGN.md` has 14 and this document has **11**, because chart B2 (the `P*` nodes) is a chart in its own right. Measured, like the 188+8 count above. | A test assertion, not behaviour — and the stricter number is the one being met. |
| 2026-08-01 | S11 / §4.6 | A chart's `title` is the **nearest preceding markdown heading**, with its numbering and any `Flowchart X — ` lead stripped. Two charts under one heading share a title (Spotlight's E/F and I/J do), which is what the document says about them. | One function (`cleanTitle`) over generated data. |
| 2026-08-01 | S11 / §4.6 | Nodes and edges carry a `line` beyond §4.6's shape, so an error or a click can name the source line. `decidedBy` is gathered from the label and from prose chunks after the chart that name both a node and a question — "best-effort", exactly as §4.6 says. | Additive fields in a generated file that is rewritten whenever the document's hash moves. |
| 2026-08-01 | S12 / §4.6 | The **server writes `graph.json`** when the document's hash moves, so an implementation agent finds a real file rather than a cache that only exists while the server runs. §2 calls the file agent-owned; the only writer is still this one process. | Delete the write and `GET /graph` still answers from the document. |
| 2026-08-01 | S12 / Q52 | **Both** halves of Q52 are built, because the owner's written answer asked for both: the whole graph with per-chart collapsing (option c) *and* a chart picker that shows one chart on its own (option a). PLAN §1 records only (c). | The picker is one column of the canvas; removing it leaves (c) intact. |
| 2026-08-01 | S13 / §4.8 | Two routes §4.8 does not list: **`GET /review`** (the annotations back out again, plus the derived assumptions and out-of-scope lists — §4.8 has POST `/annotate` but no way to read one) and **`POST /review`**, chart F10's "Review again", whose owner-half `reason` §4.2 has no value for — it is written as `review_again`. | Two routes and one enum value; no file format changes. |
| 2026-08-01 | S13 / F3 | **Out of scope is derived, not authored**: a question counts when its section heading matches "out of scope" or when the option it took is labelled "out of scope". Both design documents end with exactly such a section, which is the convention `SKILL.md` asks for. Open notes = node and edge annotations plus any note left on an answer. | Two predicates in `outOfScopeFor`; nothing is written to disk. |
| 2026-08-01 | S14 / §4.5 | `annotations.json` gains a **`flagged`** map beside `approved`. Q59's written answer is disapproval flagging with soft-approval by default — unflagged means "not objected to", which is deliberately weaker than approved — and `approved` alone cannot express it. `approved` is kept for the deliberate act. | An additive key in a UI-owned file; nothing else reads it. |
| 2026-08-01 | S14 / §4.7 | A frozen version also holds **`transcript.md`**. Q72=(a) puts the raw transcript "in a separate file" and §4.7's list has nowhere for it. | One more generated file inside a directory that is written once. |
| 2026-08-01 | S14 / §2 | **Confirm writes agent-owned files from the UI side**: `versions/NNN/` and `meta.json`'s `confirmed_version`. The one-writer rule holds in practice because Q24=(a) means the agent does not work while the owner reviews, and Confirm is the owner's act. | One route; the alternative is a status flag the agent acts on, which is the same write one hop later. |
| 2026-08-02 | S15 / §4.3 | **A gap's answer is an answer**: it is recorded in `answers.json` and `answers.log` under the gap's own ID (`GAP-002`), beside the questionnaire's. §4.3 keys that map by question ID and gaps are not questions of the document — but chart J8 says a gap *is* a draft question, and a second answer file would mean a second durability path, a second recovery contract and a second history. Gap IDs cannot collide with question IDs (`ID_PATTERN` forbids a `-`), and no gate can name one. | The key is namespaced; nothing but the gap surface reads it, and lifting it into its own file is a change of directory, not of format. |
| 2026-08-02 | S15 / §4.2 | The owner half gains one `reason`: **`gaps_answered`**, written when the last open gap of a scoped round is answered. It is what wakes the agent (S9 watches that file) to write design version N+1. The question screen deliberately does **not** treat it as the end of the questionnaire — Q88b=a parks only the affected thread — so the main round carries on where it was. | One enum value and one predicate on the screen; the alternative is the agent not being woken at all. |
| 2026-08-02 | S15 / §4.8 | Two routes §4.8 does not list: **`GET /next?scope=gaps`** (the scoped round, which Q90b=b puts in the website and §4.8 has no route for) and **`POST /promote`** (Q95b=a's "actually I do want a say in that", which files an open gap from the review canvas). `GET /gaps` grows the gaps' parsed questions, the open/closed counts and the stale-step list. | Additive routes over files that already existed. |
| 2026-08-02 | S15 / J16 | **Stale steps are computed and reported, never written.** `GET /gaps` returns them and the gap page shows them; `PLAN.md` is not edited to add a STALE marker. The plan is agent-owned (§2) and a tool that rewrites the document it reads its own citations out of is a second writer on the one file the traceability depends on. | Delete the `stale` key and the report goes with it; nothing on disk changed. |
| 2026-08-02 | S15 / J8 | A gap's prose is read **best-effort**, the same standing §4.6 gives `decidedBy`: its `**Options I can see**` line is handed to the shared grammar with `*my recommendation*` normalised to `*default*`; its `**Blast radius**` line is split at "design nodes", with `J8–J17` ranges expanded; an options line naming no `**(x)**` option at all is a gap awaiting its options, not a broken one. A gap file is never rewritten by any of this. | Every one of them is a predicate in `gaps.mjs` over a file the tool only ever reads. |
| 2026-08-02 | S2 / §5.3 | **A gate atom that appears twice collapses to one.** A question inside a gated section routinely repeats its section's gate (`- **Q9** \`[QR1=a]\`` under `### 17.2 … \`[QR1=a]\``), and `andGates` concatenated them, so the owner was told "asked because QR1 = …" twice on one screen as if there were two reasons. The conjunction of an atom with itself is that atom; `evaluateGate` was already indifferent to the duplicate. | One `Set` in `andGates`, and a test pins the collapse. |
| 2026-08-02 | S12–S13 / §10 | Six appearance defects the owner found on review were fixed as look decisions, which §10 says is where the implementer is expected to be inventing: **one** inline-markdown renderer (`web/md.mjs`) now serves all three screens, and the canvas panel — which had never had one — uses it; chart names wrap instead of truncating; the toolbar and both sidebars fold away (`f`, `1`, `2`, `3`) so the graph can have the window; a node carries a chip naming the questions that decided it, blue while any is unanswered; the detail panel names the recommended default of a question nobody has answered; and edge labels are nudged off each other with a tie back to their own edge. | Each is one function or one rule; none changes a file format, an answer, or what the graph means. |
| 2026-08-02 | S11 / §6.1 | **A `chart X` reference resolves by the name the DOCUMENT uses before the chart's ID**, and a heading names only the first chart under it. GAP-002 = (b) says to derive the links; it does not say how a name is resolved, and the two orders disagree on real data: Spotlight's §7 holds two charts, so from there on every chart ID is one letter ahead of its heading, and `K14 "see chart H"` means the chart with `I`-prefixed nodes. ID-first resolution links it to chart H — a wrong link, drawn as confidently as a right one. `name` is therefore null rather than falling back to the ID, so a fabricated name cannot shadow a real one. | One lookup order in `deriveLinks`, pinned by a test that reproduces the drift in miniature. |
| 2026-08-02 | S11 / §6.1 | An unresolved reference is a **warning**, a same-chart reference is **dropped silently**, and a repeated one is **deduped**. GAP-002 chose derivation but not its strictness. Warning follows GAP-001's rule for the same class of thing — an authoring defect reaches the author and never blocks the owner. Silent for same-chart because Spotlight's chart E says "chart E2/E3/E4" about its own nodes and that is correct authoring, not a defect. Deduped because `D8`'s label names chart E twice and repetition in prose is not a second connection. | Three predicates in one function; the counts are a fixture in `graph.test.mjs` and in PLAN §6.1. |
| 2026-08-02 | S12 / F13 | **The link's bow lives in `layout.mjs`, not the renderer, and is perpendicular with a floor and a ceiling.** Found by looking, not by reasoning: with every chart collapsed the charts shelf-pack into one row, so both ends of every link sit at the same y and the first implementation — a bow proportional to the vertical drop — drew ten straight lines lying along the row, through the boxes between their ends. A quadratic Bézier also only reaches half its control offset, so the ceiling is 260 to clear a 74-high box, measured against the picture at 120 grazing them. It is in the layout because a frozen version has to reproduce it. | One expression; the flatness floor is pinned by a test. |
| 2026-08-02 | S12 / F13 | Links are **hidden in the single-chart picker view** and carry a **toolbar toggle** (`l`, default on). The picker shows one chart, so the far end of every link is off-screen by construction and a link there is a line going nowhere. The toggle exists because the owner's report that opened GAP-002 was about clutter, and the answer to a clutter complaint should not be an unremovable new layer. | One condition in `redraw` and one boolean; neither reaches disk. |
| 2026-08-03 | S19 / B18 | **Enter's mark decides the answer STATE, not just the target.** Q12/Q108 record an Enter-accepted recommendation as `defaulted` because it was not really considered; making the target visible could have quietly turned every Enter into a `chosen`. So: the mark where the screen *put* it answers `defaulted`, the mark the owner *moved* there answers `chosen`. The distinction the questionnaire asked for survives being shown. | One branch in `pressEnter`; the states on disk are unchanged. |
| 2026-08-03 | S19 / B20 | **An option letter always wins its key, and a colliding action gets NO key on that question** rather than a rebound one. A key that presses "not relevant" on one question and picks option (n) on the next is worse than a key that is absent. Both real documents top out at three options, so this is a guard rather than a common case. | One lookup (`keyFor`); the legend is generated from the same map, so it cannot advertise a key that does not work. |
| 2026-08-03 | S19 / D12 | BACK with nothing above the current question falls back to **the question answered immediately before it**, not the newest answer. The owner asked for "the question previously answered before that question appeared"; the newest answer is a different thing and at the first question of a round it sends BACK *forwards*. Where there is genuinely nowhere to go the button is **disabled with the reason in its tooltip** — the original complaint was a button that did nothing. | One index lookup in `backTarget`, and a `disabled` flag. |
| 2026-08-03 | S19 / B19 | What the owner types is **kept per question in `sessionStorage` until it is committed**, and cleared only after the 200. The reported bug was Enter discarding a typed answer with no undo — and there was no undo because the textarea itself had been replaced, so the browser's stack was empty. Restoring the text is more reliable than trying to preserve a native undo stack across a re-render. | Two helpers over `sessionStorage`; nothing reaches disk, and a private-mode failure degrades to the old behaviour. |
| 2026-08-03 | S19 / §4.9 | **`session.json` is a heartbeat and staleness is the signal**, with a 3-second floor under the window because `now()` writes timestamps to the second. A goodbye flag alone cannot report the case that matters — a killed or crashed session never writes one. A clean exit still writes `watching: false`, so a tidy shutdown does not have to wait out the window. | One file with one writer, and a reader that is pure arithmetic over it. |
| 2026-08-03 | S19 / §4.8 | `GET /api/designs/:key?poll=1` skips `touch()`. The answering screen now asks "is anyone watching yet?" every few seconds, and without this every poll would rewrite `ui_meta.json` and wake the watch's own directory watcher several times a minute, for no information. | One query check; the un-polled route is byte-for-byte what it was. |
