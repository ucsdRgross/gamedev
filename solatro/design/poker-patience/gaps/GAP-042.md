# GAP-042 — a prop that scores a card in the ENTRANCE banks into a bucket the score never reads

status: ANSWERED and LARGELY BUILT — banking and height labels are in; the row label and
        multi-row cells wait on one scoped refactor, below
raised: the closing adversarial review of the grid run
design: `PLAN.md` §1.6 (the economy), §1.9 (the Entrance); `QR5`, `Q322`, `Q110`, `Q28`
severity: LATENT — unreachable in the shipped content, and silently loses score the moment it is reached

## THE ANSWER

Owner, verbatim:

> *"entrance should be considered part of grid. by default it should score into its own row label,
> which goes into its own row bucket as if grid is 5x6. column based score should go into shared col
> score labels between entrance and grid. height labels should go below the entrance row to prevent
> overlap with column scores in same area, since currently height scoring stacks on top of stacks.
> right above entrance is the col scores though, so we use the empty area below entrance for entrance
> height scores. special scores go into the shared single bucket for everything."*

⚠ **THE HEIGHT-LABEL HALF OF THAT WAS REVERSED.** Owner, later and verbatim:

> *"i changed my mind have heights be above stacks always even for bottom. This will result in
> separation between grid and entrance to have upper portion be column score, and right below column
> score is height score from top cells of entrance"*

> *"no special cases"*

**Height labels sit ABOVE their stack in every zone, with no exception and no branch.** The band
between the grid and the Entrance stacks two things instead: **column scores on top, and directly
beneath them the Entrance's height scores** — which are above the Entrance's own stacks, so the rule
stays uniform. The banking half of the original ruling is unchanged.

Three follow-ups settled the parts that ruling does not reach.

**Does a COLUMN now need all six cells to complete?** — **No.** A column completes on its 5 grid
cells exactly as today; an Entrance card in column *i* banks into column *i*'s shared bucket and
label. No completion rule moves.

**Do diagonals run through the Entrance row?** Owner, verbatim:

> *"no by default, but a future effect could allow it, such as one that includes the entrance row as
> part of the grid so its now 5x6"*

**Does the Entrance row score as a row automatically on every refill?** Owner, verbatim:

> *"intended if effect allows it, no by default"*

## What that means, stated once

⚠ **THIS IS ABOUT WHERE ENTRANCE SCORES BANK, NOT ABOUT DETECTING LINES IN THE ENTRANCE.** Read as
"the Entrance is now a scoring row", it would hand the player a free scored hand on every refill —
the Entrance refills to all five slots, so a row-completion rule would fire every cycle with the
hand decided by the draw. Both follow-ups rule that out **by default**.

**By default, the Entrance detects NOTHING new:**

- No Entrance ROW completion. A full Entrance is not a scored line.
- No diagonals through the Entrance row.
- Columns complete at 5 grid cells, unchanged.

**What DOES change is the destination of a score already attributed to an Entrance card** — which is
exactly what this gap was about:

| Score shaped like | Banks into |
|---|---|
| a ROW | the Entrance's **own** row bucket and row label, as if the grid were 5×6 |
| a COLUMN | that column's **shared** bucket and label — the same one the grid column uses |
| a vertical stack | that cell's bucket, which folds into **special** |
| anything special | the one **shared** special bucket |

**And a future effect may promote the Entrance into the grid**, making it genuinely 5×6 — at which
point row completion and diagonals through it both switch on. The geometry must therefore be
*extensible*, not hard-wired to exclude row 5 forever.

## Layout

Height score labels for Entrance stacks go **below** the Entrance row. Above it is the column-score
strip, and height labels currently render above their stack, so an Entrance stack's label would
collide with the column scores. The empty band under the Entrance is where they go.

## ⚠ Two things the implementation has to fix on the way

- **`GameData._scan_grid_positions` hard-codes the Entrance to GRID 0** — it builds
  `BoardCoord.new(0, c, ENTRANCE_ROW, h)` regardless of `committed_grid`. Harmless while nothing
  reads an Entrance coordinate's grid; wrong the moment a bank keyed on `(grid, index, height)`
  reads it, which is what this ruling introduces.
- **`Q28`=b said slot *i* aligns with column *i* "visually only".** Shared column buckets make that
  alignment MECHANICAL. This ruling supersedes that half of `Q28`; the other half — a card from any
  slot may still be placed in any column — is untouched.

## THE DISPLAY HALF — deferred to the geometry pass, with its requirements

Owner: *"each new row of cards should automatically get assigned score labels btw, so make sure it
is scalable if we get cases like 8x7 grids and 2x3 entrance. main difference is that lower zones
have height score labels be below the zone stack instead of above in the upper grid, since column
labels are in the center."*

### Already scalable — do NOT rebuild these

Measured in the shipped code. The pre-grid behaviour the owner remembers (*"automatically filled in
labels based on position of scored cards"*) survived into the grid version on the data side:

- **Row and column label COUNTS derive from the grid's own dimensions.**
  `_fit_children(row_labels, grid.grid_height, ...)` and `_fit_children(col_labels, grid.grid_width,
  ...)`, so an **8x7 grid already gets 7 row and 8 column labels** with no change.
- **Per-height label stacks grow from the buckets.** `line_score_levels()` returns the deepest
  height any entry reaches, and `_fill_label_stack` builds that many — labels appear as scores do.
- **Cell height labels are created lazily, one per cell that has ever scored**, keyed exactly as
  `scores_cell` is, so they already scale to any grid shape.
- **Every bucket is a coordinate-keyed dictionary created at zero on first use**, so the DATA side
  already accepts any shape, including rows that do not exist yet.

### WHAT IS BUILT

- **Banking.** An Entrance score lands in its zone's own row bucket, a column-shaped one in the
  shared column bucket, height in the cell bucket, special in the one shared bucket.
  `ScoringSection.of_row_at` resolves it from a coordinate so no caller branches.
- **The Entrance is a `GridData`.** `GameData.entrance` is the storage; `upper_zone` and
  `upper_zone_type` are views of its cells, so there is one representation and all 265 existing
  call sites work untouched. Its width derives at the accessor, so it cannot drift.
- **Height labels render, in every zone, above the stack.** `_sync_cell_score_labels` resolves a
  cell through `GameData.stack_at_banked_cell` / `coord_for_banked_cell` rather than reaching into
  `grids`, so the renderer no longer knows which zone it is drawing.
- **A panel states which buckets it draws** (`_panel_bucket_grid` / `_panel_row_offset`) instead of
  inferring it from `get_index()`, so a panel mounted outside `GridContainer` reads the right rows.
  The row offset is where *"as if grid is 5x6"* lives in the renderer.

### WHAT IS LEFT, AND WHY IT IS ONE PIECE OF WORK

**The Entrance's ROW LABEL and its multi-row CELLS both need the Entrance to render through the grid
panel builder**, which it does not yet: it still renders through the legacy tableau zone renderer,
`set_card_zone(upper_zone_right, ...)`, an `HBoxContainer` of columns with no rows.

⚠ **THE BUILDER IS ALREADY REUSABLE — THAT IS NOT THE BLOCKER.** `_create_grid_panel()` takes no
zone and `_bind_grid_panel(panel, grid)` takes a panel and a `GridData`; neither touches
`state.grids`. Binding the Entrance's zone to a panel would give it row labels, cell slots and
multi-row support with no new label code at all.

⚠ **DO NOT DELIVER THE ROW LABEL BY UN-HIDING `UpperZoneLeft`.** That gutter exists, is hidden
(`upper_zone_left.visible = false`) and still renders the retired `scores_row_upper`. Filling it
looks like a shortcut and is a trap: **a grid's row-label stacks have no fixed width** — they size
from their `BigNumberLabel` text, so the gutter is font-derived and moves as scores grow. Keeping
the Entrance aligned with the grid would mean measuring the grid panel's gutter every frame and
mirroring it, which is live-coupled layout of exactly the kind the board's geometry rules forbid.
Through a real panel, alignment is structural instead — both zones laid out by the same containers.

**The swap, in full:**

1. Build the Entrance's panel with `_create_grid_panel()`, setting `META_BUCKET_GRID` to the grid it
   is attached to and `META_ROW_OFFSET` to that grid's height. It wants RowLabels and Cells only —
   column and special scores are SHARED and belong to the grid's panel, so those two gutters are a
   per-zone option rather than always-on.
2. Mount it in `EntranceStrip` in place of `UpperZone`, and bind with `_bind_grid_panel`.
3. Move the Entrance's card visuals off `set_card_zone` onto the grid path, keeping
   `card_layer_for` routing them into `entrance_card_layer`.
4. ⚠ **Key its origin separately from grid 0's.** `_grid_panel_origin` and `_grid_cells_origin` are
   keyed by grid index and an Entrance coordinate carries `grid = 0`, so the two collide today.
   This is the real work, and it is where the separation band's budget and `GAP-039` meet.
5. Retire `upper_zone_right`, `upper_zone_left` and the `set_score_zone` legacy gutter with it.

**Do it as one pass with the band geometry**, not before: steps 4 and the band budget are the same
measurement, and doing them separately measures the focused fit twice.

### What the geometry pass has to add

1. **The Entrance gets no label stacks at all.** `RowLabels` is fitted to `grid.grid_height`, which
   covers grid rows only. The Entrance's own row bucket accumulates with nothing rendering it.
2. **The height-label ARITHMETIC does not change at all.** `_sync_cell_score_labels` already places
   above the topmost card at `at.y - card_h/2 - label_h`, and the revised ruling makes that the rule
   everywhere. `slot_center_global` already routes an Entrance coordinate to
   `_entrance_slot_center_global`, so the same expression serves both.
   ⚠ **What changes is the separation band's BUDGET.** It must hold the column-score strip and,
   directly beneath it, the Entrance's height labels. Sizing that band is the real work, and it is
   what feeds the focused-fit derivation below.
3. **`_sync_cell_score_labels` rejects any key whose row is not a grid row**, so an Entrance height
   score renders nothing even once it banks. ⚠ **FIX IT BY MAKING THE LOOKUP ZONE-GENERIC, NOT BY
   ADDING AN ENTRANCE BRANCH.** Owner: *"no special cases"*. It currently reaches through
   `state.grids[key.x]` and `grid.cell_index(...)`; the general form resolves a cell through
   whichever ZONE owns it, so the Entrance needs no mention. The Entrance is already a `GridData`
   like the grids, so the shape to iterate exists — what is missing is an accessor that hands back
   every zone rather than just `grids`. See [[general-not-shape-specific]].
4. **A MULTI-ROW ENTRANCE — SETTLED.** Owner, verbatim:

   > *"should be row. height is gained from stacking cards in one slot"*

   So the Entrance's second dimension is genuinely ROWS, and a slot's stack depth is the HEIGHT
   axis — the same meaning `h` carries in a grid cell. A 2x3 Entrance is **2 slots wide by 3 rows**,
   each slot still stacking for height on top of that.

   ⚠ **THIS CONFIRMS THE SHIPPED BANKING RATHER THAN CHANGING IT.** Today's Entrance is one row of
   five stacking slots, so `h` is already height and `of_entrance_row` is already right:
   `index = grid_height`, `height = h`. The generalisation is `index = grid_height + entrance_row`;
   the `height` half of the key does not move.

   ⚠ **What the Entrance does NOT have is a row axis.** `upper_zone` is a flat array of slots, each
   an `ArrayCardData` stack — there is nowhere to put a second row. Giving it one is a structural
   change (the Entrance becomes cell-shaped like `GridData`, width x height of cells, each cell a
   stack), not a label change. **Do not infer rows from stack depth to fake it**: that is exactly
   the conflation this ruling rejects, and it would silently turn a two-high slot into a second row.

### Why it is not done here

`RowLabels`'s minimum height IS the cell block's height — that is how its bottom edge lands on the
cells. A sixth stack breaks that invariant, changes the panel height, and feeds the focused-fit
derivation `GAP-039` is open on. `GRID LAYOUT` also asserts every row label lines up with its own
row, which a label row with no grid row would fail. Landing it inside the geometry pass measures
that fit once instead of twice.

## The defect this gap was filed for

`PropScoreProps` (Knife) and `PropScoreTalents` (Hoop) build an Entrance section with the legacy
`ScoringSection.of_line`, which leaves `section.grid == -1`, so `Game.add_line_score` takes the
legacy branch into `state.scores_row_upper` / `state.row_total` — neither of which
`GameData.live_total()` reads. The points vanish; the combo still bumps, because `register_combo()`
runs before the branch.

It is LATENT: `spawn_props()` is called only from `Game._run_score_effects` over `result.meld`, and
a meld is always a grid line, so no shipped content can put a prop on the Entrance row. But
`Game.row_slot_path` has an explicit Entrance branch built for exactly that, so the first
re-router fires it.

⚠ **Do not fix it by deleting the Entrance branch and falling through to `of_line_at`.** `v.y` is
`-1` there, so the section would collect a row that does not exist and score an empty card list —
the same loss, with the branch that documented it gone.
