# STEP BRIEF — S21: the flip (Phase 6, the last of three)

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`; HEAD = the S20
commit, `git log --oneline -1`). Work ONLY there. `GODOT_BIN` =
`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`. Read
`solatro/design/sidebar/briefs/S12.md`'s RULES section first — it binds this step. Read the
S19–S20 lines of `solatro/design/sidebar/ASSUMPTIONS.md` and their task entries in
`solatro/HANDOFF_sidebar.md`. The suite is 48 suites.
Plan: `PLAN.md` Phase 6 S21 (quoted below), §2's row "Deck viewer content", §3a's S21 row
(`Cards/card_visual.gd` (the spawn position and the flip), `UI/play_area.gd` (the Entrance strip),
`Levels/game.gd` (`refill_entrance_if_due`), `UI/deck_viewer.gd` — line numbers stale; edit by
NAME). `DESIGN.md` §4 rulings below; chart I (`I1`–`I13`). `TEST_PLAN.md` 9.5 and 9.6 are by-eye
rows; §4 has NO automated rows for this step — the rows below are the overseer's (a TEST_PLAN hole,
recorded). `NAMES.md`: knobs `entrance_stock_face_down_cap` (`int`, `5`) and
`entrance_flip_stagger` (`float`, `0.15`); string `SIDEBAR_STOCK_REMAINING` ("The face-down
stock's description: how many remain").

## The plan's text (quoted)

> **S21 — The flip** *(implements I1–I12, Q203, Q213, Q214, Q215b, Q216, Q217, Q218, Q219, Q220,
> Q221, Q223, Q244, Q245)*
> Face-down stocks with the capped depth; flip in place, staggered left to right; no fly-in;
> hovering a stock describes the slot; deck viewer shows the sorted union.
> ⚠ `Q245` fixes the face-down cap, verbatim: *"knob defaulting to 5"* — so
> `entrance_stock_face_down_cap` starts at 5.

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim

- **Q213 = (c)**: which cards flip — "all of them, staggered left to right to match the draw
  order you already ruled on".
- **Q214 = (a)**: slots still holding a face-up card at a refill — "nothing — they keep their
  card, and only the empty slots flip".
- **Q215b = (a)**: the stagger's timing — "a knob, as a fraction of `get_delay()` per slot".
  Project rule quoted in the question: "animation timings are FRACTIONS of `get_delay()`, never
  wall-clock literals". The knob is `entrance_flip_stagger`, default `0.15`.
- **Q216 = (a)**: an exhausted slot — "an empty slot frame, which is what `Q36`=(a) already ruled
  the Entrance shows".
- **Q217 = (b)**: "v1 draws up to a capped number of face-down cards, so depth reads immediately".
- **Q244 = (a)** (resolves the contradiction between Q203/Q217/Q218): "`Q203` gives way — depth IS
  visible: the stack is drawn to a cap, and hovering it gives the exact count. `Q203` only meant
  that no number is printed on the slot itself". So Q203=(b) is STALE — no number on the slot, but
  the stack depth is drawn and the hover count exists.
- **Q245** (override, owner's words): *"knob defaulting to 5"* — `entrance_stock_face_down_cap = 5`.
- **Q218 = (b)**: hovering the face-down stock — "yes — it describes the SLOT, not the card: how
  many remain in this stock".
- **Q219 = (b)**: the Deck viewer's order — "sorted by suit and rank, so no ordering information
  leaks".
- **Q220 = (a)**: "no — one list, which is what *'in total'* says". No grouping by slot.
- **Q221 = (a)**: the Discard viewer — "no" change.
- **Q223 = (a)**: a bonus reveal "simply deepens" the slot's face-up stack. NO caller exists (it is
  a future skill): build nothing for it, assert nothing; note in your report that the existing
  per-slot stack already admits it.
- I7 (chart): "there is no fly-in from the Deck control any more".

## What the audit established (verify; do not re-derive)

- Today `CardVisual` spawns Stage.DRAW cards at `deck_ui`'s centre — which since S2 is a
  ROOT-viewport control while the card lives in the picture's SubViewport (wrong space, latent).
  I7 deletes the fly-in, so that spawn path goes with it: a refilled card appears IN ITS SLOT and
  flips there.
- The flip today: `data.flipped` slerped at a hard-coded rate, only while `floating`. Reuse the
  existing flip (do not write a second one); the stagger is a delay before each slot's flip
  starts — `i * entrance_flip_stagger * get_delay()` for slot index i left to right. Use the
  engine's tween/timer, not a hand-rolled countdown. The hard-coded rate is a pre-existing
  literal: if you touch that line, the rate becomes a named knob only if the design names one
  (it does not) — leave the literal and say so.
- `deck_viewer.gd`'s sort enums are dead — sort at the CALLER (the Deck button's open path in
  `game_view.gd`), one flat union of every stock, by suit then rank. Delete the dead enums if
  nothing else reads them (grep).
- The face-down stack: up to `entrance_stock_face_down_cap` face-down visuals per slot, drawn as a
  stack (the Entrance already draws a stack per slot — reuse that layout; the cap only bounds how
  many are drawn, never how many exist). `Q245`'s note: "a deeper stack pushes the whole board
  up, since the Entrance is row −1 and its height participates in the offset sum" — measure the
  board offset before and after and report it.
- The hover on a face-down stock publishes through the SAME route as a card (`info_requested` →
  the view → `HudContainer.show_description`), with an `InfoEntry` whose title is the slot and
  whose body is `SIDEBAR_STOCK_REMAINING` with the count. Add the CSV row; translation is out of
  scope (`Q164`=a).

## TESTS OWED (overseer-defined rows; `TestEntranceStocks` for data, `TestSidebar` for the
description, whichever existing suite hosts a real `PlayArea` for the visuals — say which)

- **S21.1** no fly-in: after a refill, the new Entrance card's visual is created at its slot's
  position (spawn position == slot centre, within a pixel), never at the Deck button.
- **S21.2** only empty slots flip (Q214=a): a slot holding a face-up card at a refill is untouched
  (same visual instance, still face-up); the empty slots get a face-down visual that flips up.
- **S21.3** the stagger: with three empty slots refilled, slot i's flip starts at
  `i * entrance_flip_stagger * get_delay()` (assert the ordering AND the spacing, against the knob;
  change the knob in the test and assert the spacing follows it).
- **S21.4** the capped stack: a slot with stock depth 8 draws exactly `entrance_stock_face_down_cap`
  face-down visuals; depth 3 draws 3; depth 0 draws the empty slot frame (Q216=a). Set the knob to
  2 in the test and assert 2.
- **S21.5** hovering a face-down stock publishes a description whose body carries the remaining
  count (Q218=b) through the real route (assert `hud_container.showing_description()` and the
  count in the text); draw one card and hover again → the count drops by one.
- **S21.6** the Deck viewer shows the union of every stock as ONE list sorted by suit then rank
  (Q219=b, Q220=a): with stocks `[[K♠, 2♥], [A♠]]` the viewer's order is by suit then rank, not
  slot order; the Discard viewer's content is unchanged versus HEAD (Q221=a).
- Red-then-green for each: neutralise the behaviour (restore the deck-button spawn; flip every
  slot; zero the stagger; ignore the cap; publish nothing on a stock hover; slot-order union) and
  name which rows went red.
- **9.5 / 9.6 by eye** (`/fx-verify` rows): extend `Tests/Visual/sidebar_snapshot.gd` with
  `entrance_stocks.png` (the capped face-down stacks at the deal) and `entrance_flip_mid.png` (a
  frame captured mid-stagger: the leftmost slot flipped or flipping, the rightmost still face
  down). 9.6 is a DURATION — run it and describe what moved, in order, in your evidence file.

## DONE-WHEN (the overseer verifies each with a bounded command)

1. S21.1–S21.6 exist, red-then-green.
2. `grep -n "deck_ui" solatro/Cards/card_visual.gd` shows no spawn-position read (the fly-in is
   gone); `grep -rn "entrance_flip_stagger\|entrance_stock_face_down_cap" solatro --include=*.gd`
   shows each knob defined once in `player_settings.gd` and read at its one site.
3. `grep -n "6.5" solatro/Cards/card_visual.gd` — unchanged versus HEAD (or explain).
4. Full windowed suite: `ALL 48 SUITES ... CHECKS PASSED`, `[<=22 placeholder warnings]`, 0
   `SCRIPT ERROR`, exit profile exactly the two standing lines + `135 ObjectDB`.
5. `py .claude/tools/doc_check.py --changed`: zero findings on lines you ADDED.
6. `entrance_stocks.png` and `entrance_flip_mid.png` exist; you LOOKED and described them.

## THE CALL SITE
`Game.refill_entrance_if_due` → `TypeInput.on_refill` pops the slot's top → the Entrance strip
creates the visual face-down IN THE SLOT → the staggered flip. Delete the in-slot spawn → S21.1
red; delete the stagger → S21.3 red; delete the cap → S21.4 red.

## Rules restated
- Comment rule: column 0, above the method, at most 3 lines, says WHY. No whitespace before a
  comment, none trailing code. NO DESIGN IDS in code or comments.
- Complexity rule: engine tween/timer before hand-rolled; the existing flip and the existing
  Entrance stack layout before new ones; each thing at its proper altitude (the count belongs to
  the data, the stagger to the strip, the text to the entry). No parameter, flag or `@export`
  without a caller today (the two knobs ARE named by the design). No speculative guard.
- ⚠ Never rewrite a source through `Path.write_text` / text-mode `open('w')` (CRLF). Edit tool or
  binary writes; verify LF at the byte level on every touched file before reporting.
- One Godot process: `tasklist | findstr Godot_v4.7` empty before every run. Never kill by image
  name. Never stage or commit. Render the snapshot with the NON-console exe and wait for exit.
- Every `--import` rewrites `solatro/design/effect-review/EFFECTS.*.translation` and
  `.csv.import`: revert them (`git checkout -- solatro/design/effect-review` then
  `git clean -fq -- solatro/design/effect-review/*.translation`) before reporting.

## Evidence file
Append as you go to
`C:\Users\khanr\AppData\Local\Temp\claude\C--Users-khanr-Documents-GitHub-gamedev\17b9630a-bfc5-4952-9ab1-8c7e429751ed\scratchpad\S21_evidence.md`.
End with the fixed `STEP:` block (STEP: S21). No code or diffs in the reply. New names not in
NAMES.md → one ASSUMPTIONS.md line each ("S21: ...").
