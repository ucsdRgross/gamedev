# STEP BRIEF — S20: rebalance (Phase 6, the second of three)

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`; HEAD = the S19
commit, `git log --oneline -1`). Work ONLY there. `GODOT_BIN` =
`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`. Read
`solatro/design/sidebar/briefs/S12.md`'s RULES section first — it binds this step. Read the S19
lines of `solatro/design/sidebar/ASSUMPTIONS.md` (where the stocks live and how they are walked)
and the S19 task entry in `solatro/HANDOFF_sidebar.md`. The suite is 48 suites.
Plan: `PLAN.md` §1.7's rebalance block (quoted below), §3a's S20 row (`Levels/game.gd` (the
rebalance), `Scripts/game_data.gd` — the plan's line numbers are stale; edit by NAME), Phase 6
S20. `DESIGN.md` §4 rulings below; chart H nodes `H6`–`H12`. `TEST_PLAN.md` §4 rows 4.4, 4.5, 4.6.
`NAMES.md`: `Game.rebalance_stocks() -> void` ("Add/remove rebalance, pure rule, no RNG").

## The plan's text (quoted)

> **S20 — Rebalance** *(implements H7, H8, H9, H11, Q204, Q205, Q206, Q207, Q208, Q209, Q210)*
> §1.7's two rules. Tops never move.
> **Done-when (hard gate):** a test removes a slot and asserts every remaining slot's TOP card is
> unchanged.

§1.7's rebalance block (quoted):
> Rebalance, a pure rule with no seed:
> slot REMOVED: its cards go from ITS BOTTOM, round-robin, to the BOTTOMS of the remaining slots,
> left to right
> slot ADDED: the bottom card of each existing slot in turn, round-robin, until the new slot has
> its even share
> **TOPS NEVER MOVE.** What is about to be drawn is stable, so a peek effect stays honest. A slot
> whose stock exhausts stays empty; exhaustion never triggers a rebalance. (H7, H9, H11,
> `Q207`=a, `Q210`=c)
> when: at show start, and again whenever the SET of slots changes

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim

- **Q204 = (a)**: a slot is REMOVED — "round-robin from the removed pile's bottom into each
  remaining pile's bottom, left to right".
- **Q205 = (a)**: a slot is ADDED — "the bottom card of each existing slot in turn, taken
  round-robin until the new slot has its even share".
- **Q206 = (a)**: "no — it is a pure rule applied to the existing order, so it is replay-safe with
  no seed at all".
- **Q207 = (a)**: "never — only bottoms move, so what is about to come up is stable and a peek
  effect stays honest".
- **Q208 = (a)**: what adds or removes a slot — "a future card effect that widens or narrows the
  Entrance — this design builds the machinery and no caller". The design's own note: "(a) is a
  deliberate exception to the project's no-unrequested-generality rule, and it needs to be one on
  purpose rather than by accident". So: `rebalance_stocks()` is CALLED from the one place the slot
  set changes today (`Board.add_column` / `remove_column` on the Entrance zone, driven by
  `zone_adder.gd` — find the real seam, the audit named it in S19's brief) so that the machinery
  is wired, and nothing else invents a caller.
- **Q209 = (a)**: "no — it is bookkeeping under face-down cards, and nothing the player can see
  moves". No animation, no signal to the visuals beyond what a stock size change already emits.
- **Q210 = (c)**: one slot's stock runs out — "it stays empty, and rebalancing happens only on an
  add or remove event".
- **Q200 = (c)** (S19, standing): distribution "at show start, and again whenever the set of slots
  changes" — the "again" is THIS step, and it is the rebalance rule above, NOT a re-deal (a re-deal
  would move tops; Q207 forbids it).

## What is established (verify; do not re-derive)

- S19 put the stocks where ASSUMPTIONS says. The slot set is the Entrance zone's cells; the seam
  that changes it is `Board.add_column`/`remove_column`. Read how S19's deal reaches the stocks
  and put the rebalance at the SAME altitude (the data, `game_data.gd`/the board), with
  `Game.rebalance_stocks()` as the named entry NAMES.md gives.
- "Even share" for ADD: after the move, sizes are as an S19 deal of the same total over the new
  slot count would give — earlier slots take the extras (4.5: 3 slots of 6, add one → `[5, 5, 4,
  4]`). Pull from the bottoms round-robin starting at the leftmost existing slot; stop when the
  new slot has its share.
- REMOVE (4.4): 4 slots of 5, remove slot 1 → sizes `[7, 7, 6]`; every remaining TOP unchanged.
  The removed slot's cards leave from ITS bottom, one at a time, to the bottoms of the remaining
  slots left to right.
- Exhaustion (4.6): draining one slot fires no rebalance; the slot stays at 0; the others are
  untouched.

## TESTS OWED — `TestEntranceStocks` (existing suite from S19)

- **4.4** removing a slot moves BOTTOMS only: 4 slots of 5, remove slot 1 → every remaining
  slot's TOP card unchanged; sizes `[7, 7, 6]`.
- **4.5** adding a slot pulls bottoms round-robin: 3 slots of 6, add one → sizes `[5, 5, 4, 4]`;
  every TOP unchanged.
- **4.6** an exhausted slot stays empty: drain one slot → no rebalance fires; the slot stays at 0.
- Drive 4.4/4.5 through the REAL seam (the board's add/remove of an Entrance column), not by
  calling `rebalance_stocks()` on a hand-built array — a test that calls the rule directly cannot
  fail when the wiring is missing. If the real seam cannot be reached from a test fixture, say
  exactly why and drive the closest real caller.
- Red-then-green: neutralise (move tops instead of bottoms; re-deal instead of rebalance; rebalance
  on exhaustion) and name which rows went red for each.

## DONE-WHEN (the overseer verifies each with a bounded command)

1. 4.4, 4.5, 4.6 exist, red-then-green, in `TestEntranceStocks`.
2. `grep -n "rebalance_stocks" solatro --include=*.gd -r` shows the definition plus at least one
   PRODUCTION caller on the add/remove seam and the tests; no other caller.
3. `grep -rn "randi\|randf\|shuffle\|RandomNumberGenerator" solatro/Levels/game.gd
   solatro/Scripts/game_data.gd solatro/Scripts/board.gd` — no NEW RNG site versus `git show
   HEAD:` of each file.
4. Full windowed suite: `ALL 48 SUITES ... CHECKS PASSED`, `[<=22 placeholder warnings]`, 0
   `SCRIPT ERROR`, exit profile exactly `PagedAllocator ... WorkerThreadPool` + `15 resources still
   in use` + `135 ObjectDB`.
5. `py .claude/tools/doc_check.py --changed`: zero findings on lines you ADDED.

## THE CALL SITE
`Board.add_column`/`remove_column` on the Entrance zone → `Game.rebalance_stocks()` → the stocks'
bottoms move. Delete the call → 4.4/4.5 red (the sizes stay wrong).

## Rules restated
- Comment rule: column 0, above the method, at most 3 lines, says WHY. No whitespace before a
  comment, none trailing code. NO DESIGN IDS in code or comments.
- Complexity rule: engine method before hand-rolled (`Array.pop_front`/`push_front`, slicing),
  existing helper before a new one, each thing at its proper altitude. No parameter, flag or
  `@export` without a caller today. No speculative guard.
- ⚠ Never rewrite a source through `Path.write_text` / text-mode `open('w')` (CRLF). Edit tool or
  binary writes; verify LF at the byte level on every touched file before reporting.
- One Godot process: `tasklist | findstr Godot_v4.7` empty before every run. Never kill by image
  name. Never stage or commit.

## Evidence file
Append as you go to
`C:\Users\khanr\AppData\Local\Temp\claude\C--Users-khanr-Documents-GitHub-gamedev\17b9630a-bfc5-4952-9ab1-8c7e429751ed\scratchpad\S20_evidence.md`.
End with the fixed `STEP:` block (STEP: S20). No code or diffs in the reply. New names not in
NAMES.md → one ASSUMPTIONS.md line each ("S20: ...").
