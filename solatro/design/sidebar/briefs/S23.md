# STEP BRIEF — S23: the name popup and the map's sidebar (Phase 8, the only step)

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`; HEAD = the S22
commit, `git log --oneline -1`). Work ONLY there. `GODOT_BIN` =
`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`. Read
`solatro/design/sidebar/briefs/S12.md`'s RULES section first — it binds this step. Read the S4,
S5, S12 and Phase 3 lines of `solatro/design/sidebar/ASSUMPTIONS.md` (the map's container, the
publish route, how viewers inset) and the S23 audit facts in `solatro/HANDOFF_sidebar.md`'s
"Phase 2–5 audits" section. The suite is 48 (or 49) suites — read the count off HEAD's
`Tests/all_tests.tscn`.
Plan: `PLAN.md` Phase 8 S23 (quoted below), §2's row "Whether to keep `MapHoverPanel`" (quoted
below), §3a's S23 row (`Levels/map.gd` (`_on_node_hovered`), `UI/map_hover_panel.gd` (`get_info`
stays), NEW `UI/map_name_popup.gd` / `.tscn`). `DESIGN.md` §4 rulings below; chart K
(`K1`–`K12`). `TEST_PLAN.md` 9.8 is the by-eye row; there are NO automated rows for this step —
the rows below are the overseer's (a TEST_PLAN hole, recorded). `NAMES.md`: `UI/map_name_popup.gd`
→ `MapNamePopup` ("The small name-only popup above a map node"), `UI/map_name_popup.tscn`.

## The plan's text (quoted)

> **S23 — The name popup and the map's sidebar** *(implements K1–K12, QR7, Q130, Q131, Q132,
> Q133, Q133a, Q134, Q135, Q136, Q137, Q138, Q139)*
> Name-only popup above the node, staying put; hover fills the sidebar; click still enters; first
> touch tap behaves as hover; booster previews as a wrapping grid.

PLAN §2 (quoted): "Whether to keep `MapHoverPanel` — Keep the class as `get_info()`'s home; it
still has no scene. Do not revive its scene."

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim

- **QR7 = (a)**: the map's sidebar is "in — the map gets the small popup and publishes to the same
  sidebar".
- **Q130 = (a)**: the popup contains "the node's name only — `Show`, `Final show`, `Talent pack`,
  `Rest stop`".
- **Q131 = (b)**: it sits "directly above the node, centred".
- **Q132 = (a)**: it "stays put" once shown.
- **Q133 = (a)** with the owner's note verbatim: *"first tap with touchscreen is same as hover"* —
  "hover opens the sidebar and the small popup together; clicking still enters the node".
- **Q133a = (c)**: "no, and the sidebar simply keeps the last node's description after the pointer
  leaves, exactly as `Q32`=(a) does on the board".
- **Q134 = (a)**: booster previews are "a flowing grid that wraps to the sidebar's width, scrolled
  with the rest of the content".
- **Q135 = (a)**: hovering a preview card inside the sidebar — "no — that would replace the
  panel's own content with a card from inside it". ⚠ Chart node `K9` says the opposite ("hovering
  one of those preview cards switches the sidebar to that CARD"): the ANSWER wins, `K9` is stale —
  build Q135=(a), and record the contradiction in your report so the overseer files the doc fix.
- **Q136 = (a)**: "yes, always the same side" as the game's sidebar.
- **Q137 = (a)**: keyboard and pad — "yes — the same rule as the board; whatever is selected is
  described".
- **Q138 = (a)**: "yes — the sidebar is always listening, so the entry is always used" (no
  build-and-throw-away).
- **Q139 = (a)**: no popup on the board — "it is a map affordance for nodes that are just dots".

## What the audit established (verify; do not re-derive)

- Hover → the map's container is ALREADY routed (S4/S5: `Map._on_node_hovered` publishes to its
  `HudContainer`). This step adds the popup, the touch first-tap rule, the keep-last-description
  persistence (check whether the map's container already keeps it — the board's rule lives in
  `HudContainer`, so it may already hold), the wrapping preview grid, and the pad/keyboard cursor
  if the map lacks one (find out: `Levels/map.gd`, `WorldMapController`).
- ⚠ `map_hover_panel.tscn` EXISTS on disk (the leak canary preloads it) even though PLAN §2 says
  "it still has no scene". Do NOT instantiate it on the map and do not revive it; `get_info()` on
  the class stays the entry builder. Say in your report what the .tscn is and who loads it.
- `_on_node_hovered` is in `Levels/map.gd`. The click-enters path is `Map._on_node_entered` — do
  not touch its semantics (K4: "Travelling never costs a second click").
- The touch rule (K5): on a touchscreen the FIRST tap on a node describes (hover) and does not
  enter; the second tap on the same node enters. Detect the touch source the way S17 does
  (`InputEventScreenTouch` / emulated mouse device −1 — read S17's ASSUMPTIONS lines) and reuse
  that discrimination, do not write a second one.
- Booster previews: the entry's preview cards flow in a grid that wraps to the container's width
  (`Container`-family engine node — `HFlowContainer`/`GridContainer` — inside the description's
  scroll, not a hand-rolled layout). The description panel already scrolls (S8).
- The popup: a `MapNamePopup` scene (`UI/map_name_popup.tscn`, script `map_name_popup.gd`), one
  instance owned by the map, shown centred above the hovered node in the map's own space (the map
  lives inside a picture's SubViewport — same space rule as the map's other overlays), hidden when
  no node is hovered. Name text through the existing node-name source (whatever `get_info()`'s
  title uses); no literal strings.

## TESTS OWED (overseer-defined rows; `TestSidebar` has a real `Map` fixture with a real
`HudContainer` — extend it, or a new `TestMapSidebar` if the fixture shape demands; say which)

- **S23.1** hovering a node fills the sidebar (existing route, assert it still holds) AND shows the
  popup with the node's NAME ONLY (the popup's text == the node's name; no other text) centred
  above the node (popup centre x == node centre x within a pixel; popup bottom < node top).
- **S23.2** the popup stays put: move the pointer within the node → the popup's position is
  unchanged.
- **S23.3** a click on a node still enters it (the existing `_on_node_entered` path fires once,
  no second click needed); the sidebar keeps the last node's description after the pointer leaves
  (Q133a=c).
- **S23.4** touch: the first `InputEventScreenTouch` tap on a node describes and does NOT enter;
  a second tap on the same node enters.
- **S23.5** a booster node's preview cards are laid out in a wrapping grid: with N preview cards
  and the container's width, more than one row exists and every card's rect lies inside the
  container's inner rect (none under/over the edge); hovering one of those preview cards does NOT
  change the description (Q135=a).
- **S23.6** keyboard/pad: moving the map's selection to a node publishes that node's description
  (Q137=a) — through the real focus/selection path.
- **S23.7** no popup on the board: with the game screen active and a card hovered, no
  `MapNamePopup` is visible anywhere in the tree (Q139=a).
- Red-then-green for each: neutralise (drop the popup; let it follow; require two clicks; treat a
  touch as a click; single column; no publish on selection) and name which rows went red.
- **9.8 by eye**: extend `Tests/Visual/sidebar_snapshot.gd` with `map_popup.png` — a node hovered:
  the name popup above it, everything else in the sidebar. LOOK and describe.

## DONE-WHEN (the overseer verifies each with a bounded command)

1. S23.1–S23.7 exist, red-then-green.
2. `ls solatro/UI/map_name_popup.gd solatro/UI/map_name_popup.tscn` both exist; `grep -rn
   "map_hover_panel.tscn" solatro --include=*.gd --include=*.tscn` shows no new loader (only what
   HEAD already had).
3. `grep -n "MapNamePopup\|map_name_popup" solatro/Levels/map.gd` shows the one owner;
   `grep -rn "MapNamePopup" solatro/UI/play_area.gd solatro/Levels/game_view.gd` is empty.
4. Full windowed suite: `ALL <n> SUITES ... CHECKS PASSED` with n = HEAD's count (+1 if a suite
   was added), `[<=22 placeholder warnings]`, 0 `SCRIPT ERROR`, exit profile exactly the two
   standing lines + `135 ObjectDB` (the leak canary counts the map's nodes — a popup instance per
   hover would show up there).
5. `py .claude/tools/doc_check.py --changed`: zero findings on lines you ADDED.
6. `map_popup.png` exists; you LOOKED and described it.

## THE CALL SITE
`WorldMapController.node_hovered` → `Map._on_node_hovered` → (a) the container's
`show_description` (existing) and (b) `MapNamePopup.show_above(node)`. Delete (b) → S23.1 red.
The touch rule sits in the map's node input path before `_on_node_entered`. Delete it → S23.4 red.

## Rules restated
- Comment rule: column 0, above the method, at most 3 lines, says WHY. No whitespace before a
  comment, none trailing code. NO DESIGN IDS in code or comments.
- Complexity rule: engine container before hand-rolled layout; the existing publish route and the
  existing touch discrimination before new ones; each thing at its proper altitude. No parameter,
  flag or `@export` without a caller today. No speculative guard.
- ⚠ Never rewrite a source through `Path.write_text` / text-mode `open('w')` (CRLF). Edit tool or
  binary writes; verify LF at the byte level on every touched file before reporting.
- One Godot process: `tasklist | findstr Godot_v4.7` empty before every run. Never kill by image
  name. Never stage or commit. Revert `solatro/design/effect-review` after any `--import`. Render
  the snapshot with the NON-console exe and wait for exit.

## Evidence file
Append as you go to
`C:\Users\khanr\AppData\Local\Temp\claude\C--Users-khanr-Documents-GitHub-gamedev\17b9630a-bfc5-4952-9ab1-8c7e429751ed\scratchpad\S23_evidence.md`.
End with the fixed `STEP:` block (STEP: S23). No code or diffs in the reply. New names not in
NAMES.md → one ASSUMPTIONS.md line each ("S23: ...").
