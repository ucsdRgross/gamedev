# STEP BRIEF — S22: automatic end on goal (Phase 7, the only step)

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`; HEAD = the S21
commit, `git log --oneline -1`). Work ONLY there. `GODOT_BIN` =
`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`. Read
`solatro/design/sidebar/briefs/S12.md`'s RULES section first — it binds this step. Read the
S19–S21 lines of `solatro/design/sidebar/ASSUMPTIONS.md` (what "deck empty" is, `Q211`=a) and the
"Phase 2–5 audits" paragraph on S22 in `solatro/HANDOFF_sidebar.md`. The suite is 48 suites.
Plan: `PLAN.md` §1.8 (quoted in full below), §3a's S22 row (`Levels/game.gd`
(`place_card_in_grid` — the goal check goes after the cascade; `end_show`), `Levels/game_view.gd`
(the Goal label state) — line numbers stale; edit by NAME), Phase 7 S22. `DESIGN.md` §4 rulings
below; chart J (`J1`–`J12`). `TEST_PLAN.md` §7 rows 7.1–7.5 are this step's.

## The plan's text (quoted)

> **S22 — Auto-end on goal** *(implements J1–J12, QR5, Q91, Q101, Q102, Q102b, Q102c, Q103, Q104,
> Q106b, Q107, Q108, Q109, Q110)*
> §1.8.
> **Done-when (hard gate):** a headless test plays to the goal and asserts `show_ended` without any
> button press, and that undo returns to a live board.

§1.8 (quoted):
> after a placement has FULLY resolved (every line scored, every prop tick finished):
>     if state.has_met_goal():
>         Goal label changes state
>         pause
>         Game.end_show()          # the same flag, save and resolve the End button drives
> No declining. The End button survives for a show that can no longer be won, and is REVEALED when
> either the deck is empty or no action remains on the board. Undo rewinds an automatic end exactly
> as it rewinds a manual one. (J1–J9, `Q101`=a, `Q104`=a, `Q107`=c)

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim

- **QR5 = (a)**: the trigger is "the goal being met, and only that — the show resolves the moment
  the running total reaches the goal".
- **Q91 = (a)**: the End button — "yes — it is still the way to end a show you cannot win".
- **Q101 = (a)**: the check fires "after the WHOLE placement resolves — every line scored, every
  prop tick finished — so the show ends on a settled board". NOT (c) ("...AND any resulting
  Entrance refill has happened"): so the check sits AFTER the cascade and BEFORE
  `refill_entrance_if_due()`, and an ended show does not refill. (Log this reading in
  ASSUMPTIONS; `Q102b`/`Q102c`/`Q106b` are unanswered because their branches are skipped.)
- **Q102 = (a)**: "yes — a short pause so the final score lands visibly before the screen
  changes". Project rule: a FRACTION of `get_delay()`, never a wall-clock literal; if the design
  names no knob, use the existing pacing helper the cascade already uses at its proper altitude
  and say which.
- **Q103 = (b)**: "yes — the Goal label changes state the moment the total passes it, and then the
  show ends".
- **Q104 = (a)**: "no — reaching the goal ends the show, full stop".
- **Q107 = (c)** with the owner's note verbatim: *"end button becomes revealed when either deck
  empty, or no more possible action on board (no empty tiles)"*. The question's own gloss: "it
  REVEALS the button, it does not end the show, so `Q108`=(a) still holds". "Deck empty" is
  `Q211`=a — EVERY slot's stock empty (S19 named the predicate).
- **Q108 = (a)**: a full board does NOT end the show — "yes, unchanged".
- **Q109 = (a)**: "unchanged — undo rewinds the automatic end exactly as it rewinds a manual one,
  and the player is back on the live board".
- **Q110 = (a)**: "unchanged — resume lands on the outcome screen" (`show_ended` saved before the
  resolve).

## What the audit established (verify; do not re-derive)

- "Fully resolved" is a code position in `Game.place_card_in_grid`: after the cascade, today
  `refill_entrance_if_due()` runs and THEN `save_state()`; the whole thing is guarded on `not
  processing` with a re-entrancy guard above it. The goal check goes after the cascade completes
  and before the refill (Q101=a). `end_show()` already saves `show_ended` before the resolve
  (Q110=a) — call it, do not duplicate its body.
- `submit_button.visible` is never written today: End is always shown. Under Q107=c End starts
  HIDDEN and is REVEALED when every stock is empty OR no cell is empty. Find where the HUD's End
  button lives now (`HudContainer`'s GameHud, S2) and drive its visibility from the game's state
  through the existing `processing_changed`/state signals — not a per-frame poll.
- `test_game_headless.gd` has a test asserting a scored line does NOT resolve the show — read it;
  its goal must be pinned high enough that the automatic end does not fire inside it (say what you
  changed and why the assertion still means what it meant).
- The Goal label state (Q103=b): the existing Goal label in the HUD gets a "met" state (a theme
  colour through the palette, S2's `PALETTE` placeholder mechanism — no literal colour) the moment
  `has_met_goal()` turns true.

## TESTS OWED — `TEST_PLAN.md` §7 (a headless suite: `TestGameHeadless` or a new
`TestAutoEnd` — say which; if new, register it and the count reads 49)

- **7.1** reaching the goal ends the show with no button press: headless run, goal 10, a
  placement scoring 12 → `show_ended` true; `show_resolved` emitted once.
- **7.2** the check fires only after the WHOLE placement resolves: a placement completing 3 lines
  → `end_show` ran once, after the last line scored (and before any refill).
- **7.3** undo rewinds an automatic end: as 7.1, then undo → back on a live board, `show_ended`
  false.
- **7.4** a full board still does NOT end the show: every cell filled, goal not met →
  `show_ended` false.
- **7.5** End is REVEALED when no action remains: deck empty (every stock), no empty cells → the
  End button is visible; with one stock non-empty AND an empty cell → hidden. (4.7 from S19
  covers "one slot empty, others not → not revealed"; point at it.)
- Red-then-green for each: neutralise (skip the check; fire it mid-cascade; make undo skip the
  ended flag; end on a full board; always show End) and name which rows went red.

## DONE-WHEN (the overseer verifies each with a bounded command)

1. 7.1–7.5 exist, red-then-green.
2. `grep -n "has_met_goal" solatro/Levels/game.gd solatro/Levels/game_view.gd` shows the check at
   the post-cascade position and the label's state read; `grep -n "submit_button.visible\|
   end_button.visible" solatro/Levels/game_view.gd solatro/UI/*.gd` shows the reveal written from
   one site.
3. Full windowed suite: `ALL 48 SUITES ... CHECKS PASSED` (49 if a suite was added), `[<=22
   placeholder warnings]`, 0 `SCRIPT ERROR`, exit profile exactly the two standing lines +
   `135 ObjectDB`.
4. `py .claude/tools/doc_check.py --changed`: zero findings on lines you ADDED.
5. By eye: extend `Tests/Visual/sidebar_snapshot.gd` with `goal_met.png` (the Goal label in its
   met state, End hidden, the board settled) if reachable in under ~30 lines; otherwise say so.

## THE CALL SITE
`Game.place_card_in_grid` → cascade completes → `has_met_goal()` → label state → pause →
`end_show()`. Delete the call → 7.1 red. The End reveal: game state → the HUD's End visibility.
Delete it → 7.5 red.

## Rules restated
- Comment rule: column 0, above the method, at most 3 lines, says WHY. No whitespace before a
  comment, none trailing code. NO DESIGN IDS in code or comments.
- Complexity rule: engine method before hand-rolled (the existing `end_show`, the existing pacing
  helper, the existing signals); each thing at its proper altitude (the predicate on the data, the
  visibility on the view). No parameter, flag or `@export` without a caller today. No speculative
  guard.
- ⚠ Never rewrite a source through `Path.write_text` / text-mode `open('w')` (CRLF). Edit tool or
  binary writes; verify LF at the byte level on every touched file before reporting.
- One Godot process: `tasklist | findstr Godot_v4.7` empty before every run. Never kill by image
  name. Never stage or commit. Revert `solatro/design/effect-review` after any `--import`.

## Evidence file
Append as you go to
`C:\Users\khanr\AppData\Local\Temp\claude\C--Users-khanr-Documents-GitHub-gamedev\17b9630a-bfc5-4952-9ab1-8c7e429751ed\scratchpad\S22_evidence.md`.
End with the fixed `STEP:` block (STEP: S22). No code or diffs in the reply. New names not in
NAMES.md → one ASSUMPTIONS.md line each ("S22: ...").
