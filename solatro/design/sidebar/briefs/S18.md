# STEP BRIEF — S18: cancel (Phase 5, input — the last step of the phase)

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`; HEAD = the S17
commit, `git log --oneline -1`). Work ONLY there. `GODOT_BIN` =
`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`. Read `solatro/design/sidebar/briefs/S12.md`'s
RULES section first — it binds this step. Read S14–S17's ASSUMPTIONS.md lines and
the S15-S17 entries in `solatro/HANDOFF_sidebar.md` (their scratch evidence files died with that session). The suite is 47 suites.
Plan: `PLAN.md` Phase 5 S18 line (quoted), §1.2 (the container's transitions — "exit X, cancel,
click on bare board, or the held card leaves its cell while following → `show_hud()`"), §3a's
S18 row (`UI/play_area.gd` the input path, `Levels/main.gd` (Escape → menu/wall)).
`TEST_PLAN.md` 1.7's cancel case (S6b, landed) is the only cancel row — the rows below are the
rest (a plan hole the overseer records).

## The plan's text (quoted)

> **S18 — Cancel** *(implements E19, E20, E21, E22, Q99=b, Q100=c, Q99, Q100)*
> Held card first, description second. Escape cancels everything AND shows the menu/wall.

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim

- **Q99 = (b)**: one press of the second mouse button cancels "the held card first, then the
  description" (E20: "the HELD CARD is released first"; E21: "a second press then dismisses the
  description").
- **Q100 = (c)**: `ui_cancel` (Escape, pad B) does MORE than the second button: *"escape to view
  menu/wall should cancel everything second button would do, and show its menu/wall immediately
  as well"* (E22) — "one press both cancels and navigates, rather than cancelling first and
  navigating on a second press".
- **Q115 = (a)** (S15): "cancel disarms, and the next click on a cell does nothing" (G15) — until
  something arms again (a click on an Entrance card, Q114=a; a placement/refill, Q116/Q118).
- F9 (S17): the second mouse button is cancel-only.
- **Q64 = (a)** / B9–B11 (S6): cancel is one of the four dismissals that revert the container to
  the HUD.

## What the audit and the overseer's greps established (verify; do not re-derive)

- Escape today: `wall.gd:270-273` reads `ui_cancel` → `Wall.back_requested` →
  `Main._on_back_pressed()` (`main.gd:536`): one step back on the `FocusStack`, the wall view when
  the stack is empty. THERE IS NO "menu" DESTINATION: Q100=c's "menu/wall" IS that existing back
  step. Escape = cancel everything the second button would (held card AND description, both in
  the one press) AND the existing back, in one press.
- S6b's cancel: `PlayArea` publishes `description_dismiss_requested` on cancel and the VIEW
  decides whether the event is spent (`GameView._on_description_dismiss_requested`); a cancel
  with nothing showing is left alone so `Wall.back_requested` still fires (`Wall._unhandled_input`
  gives the focused picture first refusal through `is_input_handled()`). Under Q100=c the game
  screen must NOT consume Escape any more: it cancels its things and lets the back proceed. The
  SECOND MOUSE BUTTON is the one that stops at cancelling (and is consumed).
- ⚠ `ungrab_cards()` also hides the description today (`play_area.gd` ~1530, the audit's
  "collapsing the two steps"). Q99=b needs them SEPARATE: releasing the held card must leave a
  locked description showing; only the second press dismisses it.
- "Released" for the armed card = S15's disarm (`ungrab_cards`, nothing re-arms behind it) — the
  card returns to its slot, not lifted, not following; `armed_slot()` still reports the leftmost
  present but nothing is HELD until re-armed by a click on an Entrance card or the next
  placement/refill. Say in the evidence what a fresh show shows after one cancel.

## TESTS OWED (rows defined by the overseer; `TestSidebar` or `TestDragPlace` — say which)

- **S18.1** second mouse button, a card held and a description locked: first press → the card
  is released (slot, not held, not following, nothing armed) and the description STILL shows;
  second press → the HUD (Q99=b). Red-then-green (neutralise the ordering → both at once).
- **S18.2** second mouse button with nothing held and a description showing → the HUD; with
  nothing held and nothing showing → nothing happens and `Wall.back_requested` does NOT fire.
- **S18.3** `ui_cancel` with a card held and a description locked → ONE press releases the card,
  dismisses the description AND `Wall.back_requested` fires (Q100=c). Red on HEAD (today the
  first Escape only dismisses).
- **S18.4** releasing the held card on its own leaves a locked description up (the collapse
  fixed): drive the release through the cancel path, assert `showing_description()` still true.
- **S18.5** after a cancel-disarm, a click on a cell does nothing (S15 has a Q115 row — extend or
  point at it) and a click on an Entrance card re-arms (Q114=a) — assert the re-arm.
- The S6b cancel test(s) in `TestSidebar` (1.7's cancel case) must keep passing under the new
  ordering; if one asserted "cancel with a held card dismisses at once", re-aim it and say so.

## DONE-WHEN (the overseer verifies each with a bounded command)

1. S18.1–S18.5 exist, red-then-green.
2. `grep -n "description_dismiss_requested\|back_requested" solatro/UI/play_area.gd
   solatro/Levels/game_view.gd solatro/Levels/main.gd` shows the cancel route unchanged in shape
   (the same signals), with Escape no longer consumed by the game screen when it cancels.
3. Full windowed suite: `ALL 47 SUITES ... CHECKS PASSED`, `[<=22 placeholder warnings]`, 0
   `SCRIPT ERROR`, exit profile exactly the two standing lines + `135 ObjectDB`.
4. `py .claude/tools/doc_check.py --changed`: zero findings on lines you ADDED.
5. `Tests/Visual/sidebar_snapshot.tscn`: `cancel_first_press.png` — after ONE right-click with a
   card held and a description locked: the card back in its slot, the description still up. LOOK
   and describe.

## THE CALL SITE
The second-button and `ui_cancel` readers in `PlayArea` → the view's release/dismiss → `Main`'s
existing back. Delete the ordering → S18.1 red; consume Escape → S18.3 red.

## Evidence file
Append as you go to a scratch file of your own (name its path in your report). End with the fixed `STEP:` block (STEP: S18).
No code or diffs in the reply. New names not in NAMES.md → one ASSUMPTIONS.md line each ("S18: ...").
