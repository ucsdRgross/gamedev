# STEP BRIEF — S19: per-slot Entrance stocks (Phase 6, the first of three)

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`; HEAD = `git log
--oneline -1`). Work ONLY there. `GODOT_BIN` =
`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`. Read
`solatro/design/sidebar/briefs/S12.md`'s RULES section first — it binds this step. Read the S14–S18
lines of `solatro/design/sidebar/ASSUMPTIONS.md` and the "Phase 2–5 audits" section of
`solatro/HANDOFF_sidebar.md` (its S19–S23 paragraph is the audit of THIS step; the plan's §3a line
numbers are stale — find every site by NAME). The suite is 47 suites; this step makes it 48.
Plan: `PLAN.md` §1.7 (the whole contract — read it in full), §2's three rows "Per-slot stock
storage", "Save migration", "Deck viewer content", §3a's S19 row, Phase 6 S19. `DESIGN.md` §4 holds
the rulings quoted below and chart H (`H1`–`H15`). `TEST_PLAN.md` §4 rows 4.1, 4.2, 4.3, 4.7, 4.8
are this step's. `NAMES.md`: `Game.stock_for_slot(slot: int) -> Array[CardData]`,
`Game.rebalance_stocks()` (S20 — do not build it here), `GameData.draw_deck` DELETED as the single
deck, `Tests/Engine/test_entrance_stocks.gd` / `.tscn` → `TestEntranceStocks`.

## The plan's text (quoted)

> **S19 — Per-slot stocks** *(implements H1, H2, H3, H4, H5, H13, H14, H15, QR9, Q200, Q201, Q202,
> Q211, Q212, Q224, Q225)*
> §1.7. Replace `draw_deck`; update every walker in the same step.
> **Done-when (hard gate):** `TestEntranceStocks` asserts a deal of 23 cards across 5 slots gives
> sizes `[5,5,5,4,4]`, and that two runs from the same shuffled order produce identical stocks.

§1.7 (quoted):
> `GameData.draw_deck` is replaced by one ordered stock per Entrance slot.
> deal: shuffle ONCE (the shuffle Game.add_deck already does), then deal ROUND-ROBIN left to right,
> so earlier slots take the extras when the count does not divide
> when: at show start, and again whenever the SET of slots changes
> draw: pop from the TOP of that slot's own stock
> ⚠ **NO NEW RNG ANYWHERE.** One shuffle, then a fixed order — which is what keeps
> `_replay_pending_placement()`'s determinism argument true as written.
> Stocks are part of the board: `on_append` and every board walk reach their cards exactly as they
> reach the draw deck today. (H13, `Q225`=a)
> ⚠ **An in-flight save written before this change is DISCARDED, not migrated.** (H14, `Q224`=b)

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim (answers.json option + DESIGN §4 text)

- **QR9 = (a)**: "real — each Entrance slot owns its own ordered stock, and the single draw deck is
  replaced by them".
- **Q200 = (c)**: distribution happens "at show start, and again whenever the set of slots changes".
  (S20 builds the "again"; this step builds show start and the seam S20 calls.)
- **Q201 = (a)**: "deal round-robin left to right, so the earlier slots get the extras —
  deterministic, and it matches the left-to-right draw order you already ruled on".
- **Q202 = (a)**: "shuffled once, dealt round-robin — one RNG event, and it is the shuffle that
  already exists".
- **Q211 = (a)**: "the deck is empty" means "every slot's stock is empty" — the condition End's
  reveal (S22) and "nothing armed" both read.
- **Q212 = (a)**: discarded cards "no — unchanged; the discard is swept back into the deck only when
  the show ends" (H15: `Game.return_to_map` still sweeps).
- **Q224 = (b)**: "old in-flight saves are not resumable and are discarded". PLAN §2: "Do not write
  a migrator".
- **Q225 = (a)**: "yes — a stock is part of the board, so `on_append` and every board walk reach its
  cards exactly as they reach the draw deck today".
- **Q233 = (b)** (standing, DESIGN §1): resume-replay is legal because "there is no RNG anywhere in
  the path" — `_replay_pending_placement()`'s own comment is the contract. Keep it true.
- PLAN §2 "Per-slot stock storage": "An `Array[Array[CardData]]` on `GameData`, `@export_storage`,
  replacing `draw_deck`. Update `_iterate_board`, the flat card list, both save walkers and the
  stage verifier in the same step."

## What the audit established (verify; do not re-derive)

- Blast radius is ~3x §3a's row. Beyond `game_data.gd` (walkers: `get_card_collections()` /
  `all_card_datas()`, the stage verifier `validate()` I1/I3/I5) and `game.gd` (`add_deck` /
  `shuffle_deck`, `draw_card()`, `return_to_map()` — the run deck is REBUILT there; miss it and
  stock cards leave the run): `card_effect_api.gd` (`draw_deck()`, `draw_card()`,
  `return_to_draw_deck()`), `skill_grid_allotment.gd` (grid count from deck size),
  `skill_hungry_hippo.gd`, `type_input.gd:on_refill`, `game_view.gd` (the Deck button opens the
  deck viewer on the deck collection — S21 sorts the union; this step must keep the viewer
  showing every stock card), and eleven test files — notably `test_iterator.gd` asserting
  collection ORDER and `test_persistence_fuzz.gd`. `grep -rn "draw_deck" solatro --include=*.gd
  --include=*.tscn --include=*.tres` before you start, and again at the end: outside `archive/` the
  only survivors are names you deliberately kept (say which and why).
- `Array[Array[CardData]]` is not an expressible GDScript type: `scoring.gd` writes it as a
  comment and `card_data_array.gd` is the existing wrapper. Use the existing wrapper; do not add a
  second one.
- A top-level per-slot array on `GameData` drifts from `entrance_zone().cells`, which
  `Board.add_column/remove_column` mutate (driven by `zone_adder.gd`). DECISION (take as written,
  log in ASSUMPTIONS): the stock rides the Entrance zone's per-cell data, so the SET of slots and
  the set of stocks cannot disagree. If that reads wrong when built (a stock cannot be reached by
  the walkers the way `draw_deck` is), STOP and report — do not build a second structure.
- `on_append` is a shuffle-time hook (`game.gd`, the `add_deck` path), not a board walk. TEST_PLAN
  4.8 wants `on_append` to reach a stock card AND the stage verifier to pass with cards in stocks.
- No RunState/save version exists. "Discarded, not migrated" needs a load-time check that an old
  save (one holding a flat `draw_deck`) is refused, not crashed on. Name the reader that produces
  the case (`player_save.gd` / the resume path) in a comment — that is the one guard rule 7 allows.
  Log the mechanism in ASSUMPTIONS.
- `draw_card()` pops the BACK today — that IS "the top". Keep top = back; do not reverse an
  order and then compensate.
- 4.3 (resume-replay): `_replay_pending_placement()` dereferences `RunManager.run` unguarded —
  the fixture must populate it (audit). The assertion is "identical board, identical scoring,
  identical refill" after replaying a saved pre-placement board.

## TESTS OWED — `TestEntranceStocks` (new suite, `Tests/Engine/test_entrance_stocks.gd` + `.tscn`,
registered in `Tests/all_tests.tscn` so the count reads 48; place it in `TestSuite`'s ordering
chain the way S16 placed TestDragPlace — read that precedent)

- **4.1** 23 cards, 5 slots → sizes `[5, 5, 5, 4, 4]` (earlier slots take the extras).
- **4.2** the same shuffled order dealt twice → identical stocks, card for card.
- **4.3** a saved pre-placement board, replay the pending placement → identical board, identical
  scoring, identical refill.
- **4.7** one slot empty, others not → "deck empty" is FALSE (End is not revealed — S22 builds the
  reveal; here assert the predicate S22 will read, name it, and that nothing disarms).
- **4.8** a card in a stock → `on_append` reaches it, and the stage verifier passes.
- Every existing test that names `draw_deck` is updated in this commit and stays green. If one
  asserted an ORDER that the deal changes (`test_iterator.gd`), say what it asserted before and
  what it asserts now, and why the new assertion is the design's.
Red-then-green for every new test: neutralise the BEHAVIOUR (deal as one flat list; deal with a
new RNG; skip a walker) and name which rows went red.

## DONE-WHEN (the overseer verifies each with a bounded command)

1. 4.1, 4.2, 4.3, 4.7, 4.8 exist, red-then-green, in `TestEntranceStocks`.
2. `grep -rn "draw_deck" solatro --include=*.gd --include=*.tscn --include=*.tres | grep -v
   archive/` — every survivor is listed in your report with its reason (expected: none, or the
   `CardEffectApi.draw_deck()` name if you keep it as the API's flat VIEW of the union — say so).
3. `grep -rn "randi\|randf\|shuffle\|RandomNumberGenerator" solatro/Levels/game.gd
   solatro/Scripts/game_data.gd` shows no NEW RNG site beyond the one shuffle that existed at
   HEAD (compare against `git show HEAD:...`).
4. Full windowed suite: `ALL 48 SUITES ... CHECKS PASSED`, `[<=22 placeholder warnings]`, 0
   `SCRIPT ERROR`, exit profile exactly `PagedAllocator ... WorkerThreadPool` + `15 resources still
   in use` + `135 ObjectDB`. The leak canary (`test_leak_canary.gd`) names the walkers — it must
   still pass with stocks in place.
5. `py .claude/tools/doc_check.py --changed`: zero findings on lines you ADDED.
6. A fresh show still deals, draws, refills and plays through by the existing headless tests
   (`test_game_headless.gd`) — unchanged assertions, green.

## THE CALL SITE
`Game.add_deck`/`shuffle_deck` → the deal into per-slot stocks → `Game.draw_card()` /
`type_input.gd:on_refill` pops that slot's top → the Entrance card appears. Delete the deal → 4.1
red and the headless play-through red. Delete a walker → 4.8 / the leak canary red.

## Rules restated
- Comment rule: column 0, above the method, at most 3 lines, says WHY. No whitespace before a
  comment, none trailing code. NO DESIGN IDS in code or comments (no Qnnn/Hnn/PLAN §) — they go
  in your STEP report only.
- Complexity rule: engine method before hand-rolled, existing helper before a new one
  (`card_data_array.gd`, the existing walkers), each thing at its proper altitude. No parameter,
  flag or `@export` without a caller today. No speculative guard — the one load-time refusal
  above names its caller.
- ⚠ Never rewrite a source through `Path.write_text` / text-mode `open('w')` (CRLF). Edit tool or
  binary writes; verify LF at the byte level on every touched file before reporting.
- One Godot process: `tasklist | findstr Godot_v4.7` empty before every run. Never kill by image
  name. Never stage or commit.

## Evidence file
Append as you go to
`C:\Users\khanr\AppData\Local\Temp\claude\C--Users-khanr-Documents-GitHub-gamedev\17b9630a-bfc5-4952-9ab1-8c7e429751ed\scratchpad\S19_evidence.md`
(the red-run failure lines and counts per neutralisation, the green banner, doc_check on ADDED
lines, the LF check, the draw_deck grep survivors). End with the fixed `STEP:` block (STEP: S19).
No code or diffs in the reply. New names not in NAMES.md → one ASSUMPTIONS.md line each ("S19: ...").
