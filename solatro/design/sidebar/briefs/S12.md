# STEP BRIEF — S12: Migrate the viewers

WORKTREE: `C:\Users\khanr\Documents\GitHub\gamedev-sidebar` (branch `sidebar`). Work ONLY there. Never
touch `C:\Users\khanr\Documents\GitHub\gamedev` (the main checkout). Run every command from the
worktree path. `GODOT_BIN` = `C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64_console.exe`.

Plan: `solatro/design/sidebar/PLAN.md` — S12 in "Phase 3", plus §1.2, §1.9 (the last ⚠ paragraph
is YOUR geometry rule), §2, §3 and §3a's S12 row. Design: `solatro/design/sidebar/DESIGN.md`
(chart L node L11, L13). Names: `NAMES.md`. Assumptions so far: `ASSUMPTIONS.md` (read it — it
records every name this run invented and the shape the container/description already has).
Handoff: `solatro/HANDOFF_sidebar.md` (skim "Phase 2–5 audits" for the S12 line).

## The plan's own text for S12 (quoted)

> **S12 — Migrate the viewers** *(implements L11, Q140, Q141, Q142, Q143, Q144)*
> Deck, discard, rules and choice viewers publish to the sidebar; their own panels go. The sidebar
> sits INSIDE those full-screen viewers (`Q141`=b).

> ⚠ **`Q141`=(b) said the sidebar sits INSIDE the deck/discard/rules viewers.** With one overlay
> instance that is impossible as written. The resolution, and it preserves the answer's intent: those
> viewers are full-screen overlays that would otherwise COVER the container, so the viewer is given a
> left inset equal to `container_px` and the container stays on top. The description is therefore
> beside the viewer's cards, which is what "inside" was asking for. If that reads wrong when built,
> it is a gap — not a licence to spawn a second container.

> §3a S12 touches: `UI/deck_viewer.gd`, `UI/choice_viewer.gd`, `UI/cards_viewer.gd` (the
> `on_inspect` callback already exists), `UI/deck_builder.gd` (the `Q166`=c repair)

> §3 anti-scope: ⚠ `UI/deck_builder.gd` is the one repair that IS in scope: fix its broken preloads
> with the current classes and remove its dead code, keeping the tool (`Q166`=c).
> 1. **Do not author new card descriptions.** The sidebar ships with today's `describe_card()`
> strings (`Q160`=a).

## THE OWNER RULINGS THIS STEP IMPLEMENTS — verbatim, from answers.json + DESIGN.md §4

- **Q140 = (a)**: "yes — they publish to the sidebar and their own panels are deleted, closing the
  open `todo.md` item" · notes — "`CardsViewer.populate()` already takes an `on_inspect` callback
  these viewers pass, so the hook exists". (The question: "The deck / discard / rules viewers still
  draw their own card tooltips and publish nothing (`Q134`=(c) said every tooltip migrates; only the
  board and map were done). Are they migrated here?")
- **Q141 = (b)**: "inside, so the viewer owns its own layout" — resolved by PLAN §1.9's paragraph
  quoted above: the viewer takes an inset equal to the container, the container stays on top.
- **Q142 = (a)**: "yes, same as the other viewers" (question: "The booster choice viewer, where
  cards are picked. Same treatment?")
- **Q143 = (a)**: "no — it is available everywhere and simply stays empty where nothing publishes"
  (question: "Is there any screen where the sidebar must NOT appear?")
- **Q144 = (a)**: "no" (question: "Does the sidebar replace anything in the options / settings
  screens?") — do not touch the settings screens.
- **Q166 = (c)**: "in scope, as a repair rather than a deletion: *'update it to no longer be broken
  using new classes and removing dead code, since errors are noisy, but still want to keep the tool
  itself.'*" Design node L13: "UI/deck_builder.gd is REPAIRED, not deleted: new classes, dead code
  removed, the tool itself kept".
- Standing rulings that still bind here: **Q34 = (b)** "at the board's own card size, so it reads
  as the same object"; **Q33 = (c)** name and visual side by side at the top, then the description;
  **Q32 = (a)** / B4: pointer leaves everything, nothing locked → the container STAYS on the last
  entry; B7 / **Q60 = (c)**: with a lock, the description follows the hover and returns to the
  locked card when the pointer leaves everything.
- PLAN §2 (decided, do not re-decide): "Who owns the swap — `HudContainer` alone. `PlayArea` and
  `Map` PUBLISH `InfoEntry`s and never decide what is shown." "What `InfoEntry` looks like —
  Unchanged." A viewer is a publisher exactly like those two.

## What the audit already established (read-only facts; verify, do not re-derive)

- Only `ChoiceViewer` has a panel of its own (`%CardInfo`, filled with
  `ControlCard.describe_card(card)`). `DeckViewer` has none. The RULES viewer is `DeckViewer` on
  `rules_deck`. The discard viewer is `DeckViewer` on the discard collection.
- `PlayArea.card_info(data: CardData, card_px: Vector2) -> InfoEntry` is STATIC
  (`UI/play_area.gd:2859`) and is the ONE publish shape — build every viewer's entry with it.
  `ControlCard.size_preview_to(px)` exists. Do not write a second entry builder.
- The viewers live INSIDE a picture's `SubViewport` (they are children of the screen the picture
  hosts), so "a left inset equal to the container" is `HudContainer.container_rect()` converted
  through `WallPicture.local_rect_beside()` — the SAME conversion `Levels/menu.gd` and
  `Levels/map.gd` already use. Top case (`HudContainer.container_is_top()`): the inset is from the
  top. Reuse those helpers; do not restate the geometry.
- The route to the container: `PlayArea.info_requested` → `GameView` relays →
  `hud_container.show_description(entry)`; the map's `Map._on_node_hovered` publishes to ITS
  container; `PlayArea.highlight_cleared` → `HudContainer.return_to_lock()` when the pointer leaves
  every card. Follow that route: a viewer emits to the VIEW that opened it (`GameView` for
  deck/discard/rules, whichever screen hosts the choice viewer — find it: `Levels/map.gd`,
  `Cards/Types/booster_template.gd`, `UI/deck_picker.gd` all reference viewers), and the view
  relays exactly as it relays the board's. The viewer never reaches the container itself.
- `deck_builder.tscn` is ALSO broken (a missing `ext_resource`), not only the `.gd`. Repair both.
  `Scripts/player_save.gd` references it — check what it expects.
- `Scripts/leak_sentinel.gd` and `Tests/Engine/test_leak_canary.gd` reference the viewers: the
  exit-time profile must stay exactly the two standing lines (`PagedAllocator ...
  WorkerThreadPool` + `15 resources still in use`) plus the `135 ObjectDB` note. Anything more is a
  leak you introduced: attribute it with `Tests/Support/leak_probe.tscn -- <suite.tscn>`.
- Existing suites that touch this code: `Tests/UI/test_ui_viewers.gd` (`TestUIViewers`), and
  `Tests/Wall/test_sidebar.gd` (`TestSidebar`, 68 tests, has real `GameView`/`Map` fixtures with a
  real `HudContainer` — copy their fixture shape). `todo.md:487-493` is the open item Q140 closes;
  delete that item when it is closed.

## Decisions the design does not spell out — take them as written, log each in ASSUMPTIONS.md

1. **Preview size in a viewer**: Q34=b's "the board's own card size, so it reads as the same object"
   means, for a viewer, the size THAT VIEWER draws its cards at, in window px (the object the
   player is pointing at is the viewer's card). Log it citing L11 and Q34.
2. **Viewers publish highlights only** — hover and key/pad focus call `show_description`; a click
   in a viewer is its own action (take, close) and does NOT lock. Lock/B5 is a board transition.
   Log it citing L11, B5.
3. **Closing a viewer announces "the pointer left everything"** through the same channel the board
   uses, so a description locked on a board card BEFORE the viewer opened comes back (B7), and an
   unlocked container stays on the last viewer entry (B4). Log it citing B4/B7.
If any of these reads wrong when built (the description beside the viewer's cards does not look
"inside"), STOP and report — that is the one gap PLAN §1.9 names. Do not spawn a second container.

## DONE-WHEN (all of it; the overseer verifies each with a bounded command)

1. `grep -rn "CardInfo" solatro/UI/choice_viewer.gd solatro/UI/choice_viewer.tscn` returns nothing
   (`map_hover_panel.*` keeps its own `CardInfo` — not yours).
2. Every viewer publishes: deck, discard, rules (all `DeckViewer`) and choice. Proven by tests that
   FAIL when the relay is removed (see tests below, red-then-green each).
3. The viewer's card area starts at the container's inner edge converted into the picture's space
   (side case) — proven by a test, not by eye alone.
4. `load("res://UI/deck_builder.tscn")` instantiates in a test with ZERO `SCRIPT ERROR` in
   `%APPDATA%\Godot\app_userdata\Solatro\logs\godot.log`, and the repaired `deck_builder.gd` has no
   dead code (a function nothing calls, a preload nothing reads).
5. `Tests/Visual/sidebar_snapshot.gd` gains `viewer_description.png`: the DECK viewer open on the
   game screen, one viewer card hovered, its description in the container BESIDE the viewer's cards
   (none of the viewer's cards under the container). Add `choice_viewer_description.png` too if the
   choice viewer can be reached from the snapshot in under ~40 lines; otherwise say so.
6. Full windowed suite green: `ALL 45 SUITES ... CHECKS PASSED`, `[<=22 placeholder warnings]`,
   0 `SCRIPT ERROR` in godot.log, exit-time profile exactly the standing lines above.
7. `py .claude/tools/doc_check.py --changed` — zero findings on lines you ADDED. (Owner ruling: the
   legacy comment backlog in touched files stays; findings on old lines are expected and are NOT
   yours to fix. Do not sweep old comments.)
8. `todo.md`'s "Card descriptions are unreachable in the deck / discard / rules viewers" item is
   deleted (it is closed by this step).

## TESTS OWED (TEST_PLAN.md has no numbered rows for S12 — these are the rows; use these ids)

Put them in `TestSidebar` unless the fixture already lives in `TestUIViewers`:
- **S12.1** deck viewer: a real `GameView` fixture with a real `HudContainer`; open the deck viewer
  through the SAME call the Deck button makes; drive one viewer card's inspect through
  `CardsViewer`'s own `on_inspect` path (focus or hover, the way a player reaches it); assert
  `hud_container.showing_description()` and the title reads that card's name. RED-THEN-GREEN by
  neutralising the relay in the view (not the test).
- **S12.2** rules viewer and discard viewer: the same assertion through their own buttons/openers.
- **S12.3** choice viewer: same, in whichever screen hosts it (real container).
- **S12.4** inset: with the deck viewer open in the side case, the viewer's cards region's left
  edge in the picture's space ≥ `local_rect_beside(container_rect())`'s x; nothing of the viewer's
  card grid lies under the container. Neutralise the inset → red.
- **S12.5** lock survives a viewer: lock a board card, open the deck viewer, hover a viewer card
  (description follows), close the viewer → the container shows the locked card again.
- **S12.6** `deck_builder.tscn` loads and instantiates; the suite's engine-error scan stays at 0.
- **S12.7** the ChoiceViewer panel is gone: no `CardInfo` node in `choice_viewer.tscn` (assert
  through the instantiated scene, `find_child("CardInfo", true, false) == null`, so it is a real
  check, not a grep).
Every new test: neutralise the behaviour, watch THAT test's checks fail (confirm the red run failed
the checks you expected — not the test aborting), restore, watch it pass, and record both banner
lines in the evidence file.

## THE CALL SITE — "where is this called from, and what breaks if it is deleted?"

- The viewers' `on_inspect` callback (`UI/cards_viewer.gd:24 populate(cards, on_inspect)`) is the
  hook. Each viewer passes a callable that now builds `PlayArea.card_info(data, px)` and emits it
  to its opener. Delete the emit → S12.1–S12.3 go red. Delete the view's relay → the same tests go
  red. Delete the inset → S12.4 goes red. If any of those deletions leaves the suite green, the
  test is not testing the wire — fix the test.
- Never accept your own "done" on a publisher whose consumer you did not exercise in a test.

## RULES (repeated because they are violated every time they are not)

- **Comment rule**: a comment sits at column 0, above the method, at most 3 lines, and says WHY the
  method exists. No whitespace before a `#`; no comment trailing code; a `##` doc comment is one
  line. Wanting an inline comment means the code needs a NAME — extract a well-named helper.
- **Complexity rule**: an engine method before a hand-rolled one; an existing project helper before
  a new one (`PlayArea.card_info`, `WallPicture.local_rect_beside`, `HudContainer.container_rect /
  container_is_top`, `ControlCard.size_preview_to`, the `TestSidebar` fixtures); each thing at its
  proper altitude. Duplicated logic is a defect. A new function needs two call sites or a test that
  needs the seam.
- **No speculative defense** (repo rule 7): no guard, `try`, null check or fallback unless you can
  NAME the caller that produces the case. A precondition is an `assert`.
- **No unrequested generality** (rule 8): no parameter, flag or `@export` without a caller today.
- **No design ids in code** — no `Q141`, `L11`, `S12`, `PLAN §1.9` in a comment, string or
  `@export_group`. Test assertion messages are the one exemption.
- **No tunable literal in a source file.** Sweep your touched files for numeric/colour literals at
  the end.
- **Type every array and every for-loop variable** — warnings are errors.
- **Identifiers**: NAMES.md fixes what it fixes; every NEW name you must invent (a viewer signal,
  a method) follows the existing convention (`info_requested`, `highlight_cleared`) and is recorded
  as one line in `solatro/design/sidebar/ASSUMPTIONS.md` in that file's own style ("S12: ...").
- **Never commit, stage or stash.** Never run `git checkout`/`reset` on tracked files.
- **One Godot process at a time.** Before EVERY suite run: `tasklist | findstr Godot_v4.7` must
  show nothing of yours still running (ignore PID 3020, a stale 4.1.2 window that is not yours and
  that you must NOT kill). Never kill a process by image name — only a verified `-Id <pid>`.
- **Every `--import` rewrites `solatro/design/effect-review/EFFECTS.*.translation` and
  `.csv.import`** — do not touch those; the overseer reverts them.
- Suite: `py solatro/Tools/run_tests.py` from the worktree (~4 min, windowed, needs an explicit
  timeout ≥ 480 s). Inner loop: `--filter Sidebar` / `--filter UIViewers` (never a verdict) or
  `--logic`. Snapshot: run `Tests/Visual/sidebar_snapshot.tscn` with the NON-console exe
  (`C:\Users\khanr\Desktop\Godot_v4.7.2-stable_win64.exe --path solatro
  res://Tests/Visual/sidebar_snapshot.tscn`), wait for it to exit, and LOOK at the PNG under
  `%APPDATA%\Godot\app_userdata\Solatro\sidebar_snapshot\` — describe what it shows in your report.

## EVIDENCE FILE — append as you go, because a turn-cap stop loses your final report

Append every banner line, every red/green pair, every grep result and the by-eye description to
a scratch file of your own (name its path in your report)
the moment you have it. Do this FIRST for each result, before moving on.

End with the fixed `STEP:` block from your definition. No code, no diffs in the reply.
