@tool
class_name WorldMap2D
extends Node2D

## Drag-and-drop deliverable of the worldgen addon: a @tool Node2D that generates
## a static colorized 2D map + an interactive DAG overlay centered on origin.
##
## STUB (Phase 4): only the type is registered so the addon can be enabled and the
## node dropped into a scene. The generation/overlay/bake implementation lands in
## Phase 6 (exports, tool buttons, WorldGenerator worker, Sprite2D map, GraphOverlay,
## image outputs, EXR/bake, recolor_regions() WFC hook).
