# godot-cpp is a shallow clone, not vendored (see BUILD.md to restore it)
godot-cpp/
# MSVC build artifacts
src/*.obj
*.pdb
# SCons build database (machine-local state; currently still tracked -- ignoring
# takes effect once it is untracked, e.g. removed via GitHub Desktop)
.sconsign.dblite
